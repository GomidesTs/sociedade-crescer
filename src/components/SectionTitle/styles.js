import styled from 'styled-components'

export const Container = styled.h3`
    font-style: normal;
    font-weight: 400;
    font-size: 4.8rem;
    line-height: 140%;
    color: ${({ theme }) => theme.COLORS.BLUE};

    padding-top: 6rem;
`